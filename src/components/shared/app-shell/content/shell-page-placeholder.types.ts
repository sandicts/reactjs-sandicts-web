import type { LucideIcon } from "lucide-react";

type ShellPagePlaceholderProps = Readonly<{
  eyebrow: string;
  title: string;
  description: string;
  Icon: LucideIcon;
}>;

export type { ShellPagePlaceholderProps };
