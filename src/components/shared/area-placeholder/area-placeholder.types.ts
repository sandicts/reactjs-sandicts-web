import type { LucideIcon } from "lucide-react";

type AreaPlaceholderProps = {
  eyebrow: string;
  title: string;
  description: string;
  Icon: LucideIcon;
};

export type { AreaPlaceholderProps };
